
export interface GlobalInfo{
    slug: string;
    title:string,
    description:string,
    text:string,
    covers:{
        output_path:string,
        path:string,
        cover_type:number
    }
}